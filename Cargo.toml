[package]
name = "cuda-narrative"
version = "0.1.0"
edition = "2021"
description = "Narrative construction — arc templates, theme extraction, similarity"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Lucineer/cuda-narrative"
readme = "README.md"
keywords = ["ai", "fleet", "agent", "rust"]
categories = ["science"]

[dependencies]
serde = { version = "1", features = ["derive"] }
